services:
  - type: web
    name: airiks-resort
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: 'PORT=3001 node api/server.mjs & node .output/server/index.mjs'
    healthCheckPath: /
